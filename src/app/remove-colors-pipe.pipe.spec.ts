import { RemoveColorsPipePipe } from './remove-colors-pipe.pipe';

describe('RemoveColorsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveColorsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
