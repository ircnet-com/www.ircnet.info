import { OrderBySIDPipe } from './order-by-sid.pipe';

describe('OrderBySIDPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBySIDPipe();
    expect(pipe).toBeTruthy();
  });
});
