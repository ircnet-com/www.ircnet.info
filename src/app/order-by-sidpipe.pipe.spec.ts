import { OrderBySIDPipePipe } from './order-by-sidpipe.pipe';

describe('OrderBySIDPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBySIDPipePipe();
    expect(pipe).toBeTruthy();
  });
});
